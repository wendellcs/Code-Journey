export type Class = {
    id: string
    module: string
    day_of_week: string
    class_time: string
    created_at: string
}

export type ClassBasicData = {
    id: string
    module: string
    day_of_week: string
    class_time: string
}